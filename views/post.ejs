<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/forum.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Titillium+Web:ital@1&display=swap" rel="stylesheet">
    <title>Post</title>
</head>
<body>
<div class="container">
    <div class="subforum">
        <div class="subforum-title">
            <h1>Information du Post</h1>
        </div>
        <div class="subforum-row">
            <div class="subforum-icon subforum-column center">
                <i class="fa fa-user-circle" aria-hidden="true"></i>
            </div>
            <div class="subforum-description subforum-column">
                <h1>Titre : <%= post.title %></h1>
                <h2>Contenu : <%= post.contenu %></h2>
                <div>Poste par : <%= post.name %></div>
                <% if (type != null) { %>
                <% if (type == 'up') {%>
                <a style="color: blue;" href="/add/<%= id%>/up">UpVote</a>
                <% } else { %>
                <a href="/add/<%= id%>/up">UpVote</a>
                <% } %>
                <% } %>
                <div><%= post.score %></div> 
                <% if (type != null) { %>
                <% if (type == 'down') {%>
                <a style="color: blue;" href="/add/<%= id%>/down">DownVote</a>
                <% } else { %>
                <a href="/add/<%= id%>/down">DownVote</a>
                <% } %>
                <% } %>
                
                <% for(var i=0; i<response.length; i++){ %>
                    <h2><%= response[i][0].contenu %></h2>
                    <div><%= response[i][0].name %></div>
                    <% if (type != null) { %>
                        <% if (response[i][2] == 'up') {%>
                            <a style="color: blue;" href="/addr/<%= id%>/<%= response[i][1] %>/up">UpVote</a>
                        <% } else { %>
                            <a href="/addr/<%= id%>/<%= response[i][1] %>/up">UpVote</a>
                        <% } %>
                    <% } %>
                    <div><%= response[i][0].score %></div> 
                    <% if (type != null) { %>
                        <% if (response[i][2] == 'down') {%>
                            <a style="color: blue;" href="/addr/<%= id%>/<%= response[i][1] %>/down">DownVote</a>
                        <% } else { %>
                            <a href="/addr/<%= id%>/<%= response[i][1] %>/down">DownVote</a>
                        <% } %>
                    <% } %>
                <% } %>

                <% if(account){ %>
                    <form method="post">
                        <input type="text" placeholder="Contenu" required name="contenu">
                        <button type="submit">Répondre</button>
                    </form>
                <% } %>
            </div>
        </div>
    </div>
</body> 
</html>